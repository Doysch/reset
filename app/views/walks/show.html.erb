<div class="container main-container">
  <div>
  <%= image_tag 'logo.png', class:'logo-walk-show mx-4 mb-5 my-5' %>
  </div>
  <div class=" my-3 mt-3" style="width: 100%; height: 300px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-search-value="<%= [@walk.latitude, @walk.longitude].to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

  <div class="d-grid gap-2">
  <%= link_to 'Begin my reset', edit_greenspace_walk_path, class:"btn btn-search mb-5 my-5 mx-5" %>
  </div>
</div>
